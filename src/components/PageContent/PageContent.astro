---
const { title, content } = Astro.props;
---

<h2 class="mb-6 text-3xl font-bold">{title}</h2>
{content.map(section => (
  <section class="mb-6">
    {section.sectionTitle && <h3 class="mb-2 text-xl font-semibold">{section.sectionTitle}</h3>}
    <p>{section.text}</p>
  </section>
))}
